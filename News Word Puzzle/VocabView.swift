import SwiftUI

struct VocabView: View {
    var body: some View {
        VStack {
            Text("Saved and missed words will appear here.")
                .foregroundColor(.secondary)
        }
        .navigationTitle("Vocabulary")
    }
}
